<%- include("partials/header") %>

<h2>Total Productos: <%= totalProducts %></h2>
<h2>Valor Total: $<%= totalValue.toFixed(2) %></h2>

<h3>Productos por Categoría:</h3>
<ul>
  <% for (let category in categories) { %>
    <li><%= category %> → <%= categories[category] %></li>
  <% } %>
</ul>

<a href="/sync/products">Sincronizar productos</a>

</body>
</html>